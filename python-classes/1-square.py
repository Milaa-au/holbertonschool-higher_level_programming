#!/usr/bin/python3
"""It is a class named Square that defines a square."""


class Square:
    """We initialize a private instance attribute named size, which will be the size of our square."""
    def __init__(self, size):
        self.__size = size
